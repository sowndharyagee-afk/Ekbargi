name: ekbargi
description: EKbargi - Citizen complaint and community forum app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_dotenv: ^5.1.0  # ← YE TERA PURANA, RAHNE DE
  image_picker: ^1.1.2    # ← YE NAYA ADD KAR
  intl: ^0.19.0           # ← YE BHI NAYA ADD KAR

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
